import React from 'react';
import TokenDecoder from '../utils/TokenDecoder'; // Replace with the actual path to your TokenDecoder component

const Profile = () => {
  return (
    <div>
      <h1>User Profile</h1>
      {/* Other profile content */}
      <TokenDecoder />
    </div>
  );
};

export default Profile;
